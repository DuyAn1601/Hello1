@model IEnumerable<QLNH_TH.Models.khachhangModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tenk)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ngaysinh)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.diachi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tenk)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ngaysinh)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.diachi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.makh">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.makh">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.makh">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
